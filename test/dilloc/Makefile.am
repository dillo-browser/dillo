AM_TESTS_ENVIRONMENT = env \
	TOP_SRCDIR='$(top_srcdir)' \
	TOP_BUILDDIR='$(top_builddir)' \
	SRCDIR='$(srcdir)' \
	BUILDDIR='$(builddir)'

LOG_COMPILER = $(srcdir)/driver.sh

TESTS = \
	cmd-load.sh \
	cmd-load-multi.sh

EXTRA_DIST = \
	$(TESTS) \
	driver.sh \
	test.html
